apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reforma.fullname" . }}-test-connection"
  labels:
    {{- include "reforma.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reforma.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
